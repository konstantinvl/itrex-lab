import styled from 'styled-components';

const StyledToastTitle = styled.span`
    font-weight: 600;
    font-size: 17px;
    line-height: 24px;
    display: flex;
    align-items: center;
    color: #ffffff;
`;

export default StyledToastTitle;
