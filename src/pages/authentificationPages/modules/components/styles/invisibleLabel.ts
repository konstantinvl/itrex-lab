import styled from 'styled-components';

const StyledInvisibleLabel = styled.label`
    margin: 0;

    visibility: collapse;
    height: 0px;
    width: 0px;
    position: relative;
`;

export default StyledInvisibleLabel;
