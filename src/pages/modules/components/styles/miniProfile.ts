import styled from 'styled-components';

const StyledMiniProfile = styled.div`
    display: flex;
    flex-direction: row;
    justify-content: space-between;
    height: 40px;
    width: 208px;
`;

export default StyledMiniProfile;
