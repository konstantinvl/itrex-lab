import { Form } from 'formik';
import styled from 'styled-components';

const StyledFormAuth = styled(Form)`
    width: 100%;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    justify-content: center;
`;

export default StyledFormAuth;
