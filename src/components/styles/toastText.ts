import styled from 'styled-components';

const StyledToastText = styled.span`
    font-weight: 500;
    font-size: 13px;
    line-height: 130%;
    /* or 17px */

    display: flex;
    align-items: center;
    flex-wrap: wrap;
    width: 330px;
    color: #ffffff;
`;

export default StyledToastText;
