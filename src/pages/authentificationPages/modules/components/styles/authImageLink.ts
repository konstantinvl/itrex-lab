import { Link } from 'react-router-dom';
import styled from 'styled-components';

const StyledImgLink = styled(Link)`
    margin-right: 16px;
    height: 24px;
    width: 24px;
    background-position: center;
    background-repeat: no-repeat;
    cursor: pointer;
`;

export default StyledImgLink;
